{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run start:dev",
  "installCommand": "npm install",
  "framework": "nestjs",
  "routes": [
    { "src": "/(.*)", "dest": "/dist/main.serverless.js" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}