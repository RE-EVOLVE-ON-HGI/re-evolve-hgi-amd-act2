apiVersion: v1
kind: Service
metadata: { name: hgi-api, namespace: hgi }
spec:
  selector: { app: hgi-api }
  ports:
    - { name: http, port: 80, targetPort: 4000 }
    - { name: grpc, port: 50051, targetPort: 50051 }
